# Ollup Readme

`ollup` is a simple Bash script that updates all of your Ollama models. `ollup`
requires Bash, Awk, and Ollama.

## Limitations

Currently, this script does not update models with custom Modelfiles.
